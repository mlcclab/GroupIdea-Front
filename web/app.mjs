import {escapeHTML as e, renderMarkdown, currentWeek, titleFromMarkdown, validateUpload, makeReport, dateLabel, weekLabel, roleLabel, displayAuthor} from './core.mjs';
import {GroupIdeaService} from './service.mjs';
import {weeklyTemplate} from './template.mjs';

const config = window.GROUPIDEA_CONFIG || {};
const app = document.querySelector('#app');
const service = new GroupIdeaService();
let toastTimer;
const recordLabel = record => record.recordDate || (record.week ? weekLabel(record.week) : dateLabel(record.submittedAt));
let state = {user:null,restoring:Boolean(service.accessToken),route:'mine',file:null,title:'',week:currentWeek(),records:[],posts:[],files:[],page:0,busy:false,preview:false,error:'',mineError:'',sharedError:'',pending:null,sort:'time',authorFilter:'',browsePage:1,pageSize:10,members:[]};
const icons = {book:'<path d="M4 4h6a3 3 0 0 1 3 3v14a4 4 0 0 0-4-3H4z"/><path d="M20 4h-4a3 3 0 0 0-3 3v14a4 4 0 0 1 4-3h3z"/>',upload:'<path d="M12 16V3m-5 5 5-5 5 5M4 15v5a1 1 0 0 0 1 1h14a1 1 0 0 0 1-1v-5"/>',people:'<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2m20 0v-2a4 4 0 0 0-3-3.9M16 3a4 4 0 0 1 0 8"/><circle cx="9" cy="7" r="4"/>',arrow:'<path d="M5 12h14m-6-6 6 6-6 6"/>',file:'<path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8zm0 0v6h6M8 13h8m-8 4h5"/>',check:'<path d="m5 12 4 4L19 6"/>',back:'<path d="M19 12H5m6-6-6 6 6 6"/>',exit:'<path d="M9 4H4v16h5m6-12 4 4-4 4M9 12h10"/>',download:'<path d="M12 3v12m-5-5 5 5 5-5M4 17v4h16v-4"/>'};
const icon = name => `<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">${icons[name] || icons.file}</svg>`;
const brand = `<span class="brand-symbol">G<span>i</span></span><span class="brand-word">GroupIdea<span>随时记录思考</span></span>`;
const isManager = user => user?.role === 'admin';
const isVisitor = user => user?.role === 'visitor';
const roleOrder = {pi:0,admin:0,postdoc:1,graduate:2,student:2,visitor:3};
function toast(message) { const node = document.querySelector('#toast'); node.textContent = message; node.classList.add('visible'); clearTimeout(toastTimer); toastTimer = setTimeout(() => node.classList.remove('visible'),4500); }
function errorBox(message) { return message ? `<div class="error" role="alert">${e(message)}</div>` : ''; }
function empty(title,description) { return `<div class="empty">${icon('book')}<h3>${title}</h3><p>${description}</p></div>`; }
function download(content,name) { const url=URL.createObjectURL(new Blob([content],{type:'text/markdown;charset=utf-8'})); const a=document.createElement('a'); a.href=url; a.download=name.replace(/[\\/\x00-\x1f]/g,'_'); a.click(); setTimeout(()=>URL.revokeObjectURL(url),1000); }
function sortedPosts() { return [...state.posts].sort((a,b)=>state.sort==='name' ? String(a.displayName||a.author).localeCompare(String(b.displayName||b.author),'zh-CN') : state.sort==='role' ? (roleOrder[a.role]??9)-(roleOrder[b.role]??9)||String(a.displayName||a.author).localeCompare(String(b.displayName||b.author),'zh-CN') : Date.parse(b.publishedAt)-Date.parse(a.publishedAt)); }
function visiblePosts() { const posts=sortedPosts().filter(post=>!state.authorFilter||post.author===state.authorFilter); const totalPages=Math.max(1,Math.ceil(posts.length/state.pageSize)); if(state.browsePage>totalPages)state.browsePage=totalPages; return {posts,totalPages}; }
function render() { app.innerHTML = state.user ? shell() : state.restoring ? restoringPage() : login(); bind(); }
function restoringPage() { return `<main class="login-layout" id="main"><section class="login-story"><div class="brand">${brand}</div><div class="story-body"><div class="eyebrow">${e(config.groupName||'MLCCLab')} 思路笔记</div><h1>正在恢复你的<br>思考空间。</h1><p>正在验证登录状态，请稍候。</p></div><div class="story-footer">个人记录 · 组内交流 · 持续积累</div></section><section class="login-panel"><div class="login-card"><div class="eyebrow blue">请稍候</div><h2>正在恢复登录状态</h2><p class="muted">你的登录仍然有效，马上进入思考空间。</p></div><footer>GroupIdea <span>Copyright 2026 Jingbai Li All Rights Reserved.</span></footer></section></main>`; }
function passwordField(id,autocomplete,placeholder) { return `<div class="password-field"><input id="${id}" type="password" autocomplete="${autocomplete}" minlength="6" maxlength="128" required placeholder="${placeholder}"><button type="button" class="text-button password-toggle" data-password-toggle="${id}">显示密码</button></div>`; }
async function loadShared() { state.sharedError=''; try{state.posts=await service.published();}catch(error){state.posts=[];state.sharedError=`暂时无法读取组内总结：${error.message}`;} }
async function chooseFile(file) { if(!file||state.busy)return;try{if(file.size>512*1024)throw new Error('文件超过 512 KB，请精简内容后再提交。');const content=new TextDecoder('utf-8',{fatal:true}).decode(await file.arrayBuffer());validateUpload(file.name,content,file.size);state.file={name:file.name,size:file.size,content};state.title=titleFromMarkdown(content,file.name);state.error='';state.pending=null;state.preview=false;}catch(error){state.file=null;state.pending=null;state.error=error instanceof TypeError?'无法读取文本，请将文件另存为 UTF-8 编码。':error.message;}render(); }
function updatePaste(text) { const size=new TextEncoder().encode(text).length;state.file=text?{name:'粘贴内容.md',size,content:text}:null;state.pending=null;if(text&&!state.title)state.title=titleFromMarkdown(text,'粘贴内容.md');const form=document.querySelector('#upload-form');if(form){form.querySelector('[data-action="preview"]').disabled=!state.file;form.querySelector('button[type="submit"]').disabled=!state.file;} }
function bind() { app.querySelectorAll('a[href="#mine"],a[href="#others"],a[href="#admin"]').forEach(link=>link.addEventListener('click',event=>{event.preventDefault();if(state.busy){toast('正在处理，请稍候。');return;}state.route=link.hash==='#admin'&&isManager(state.user)?'admin':link.hash==='#others'?'others':'mine';history.replaceState(null,'',link.hash);state.route==='admin'?loadMembers():render();}));document.querySelector('#member-form')?.addEventListener('submit',connect);document.querySelector('#login-mode')?.addEventListener('change',event=>{const visitor=event.target.value==='visitor';const usernameInput=document.querySelector('#username');if(usernameInput){usernameInput.required=!visitor;usernameInput.placeholder=visitor?'游客用户名（可留空）':'管理员分配的用户名';}const submitButton=document.querySelector('#login-submit');if(submitButton)submitButton.innerHTML=visitor?'进入共享内容 '+icon('arrow'):'登录 '+icon('arrow');});document.querySelector('#member-edit-form')?.addEventListener('submit',saveMember);document.querySelectorAll('[data-password-toggle]').forEach(button=>button.addEventListener('click',()=>{const input=document.querySelector('#'+button.dataset.passwordToggle);input.type=input.type==='password'?'text':'password';button.textContent=input.type==='password'?'显示密码':'隐藏密码';}));app.querySelectorAll('[data-toggle-user]').forEach(b=>b.addEventListener('click',()=>toggleMember(b.dataset.toggleUser)));app.querySelectorAll('[data-delete-user]').forEach(b=>b.addEventListener('click',()=>deleteMember(b.dataset.deleteUser)));document.querySelector('#file-input')?.addEventListener('change',event=>chooseFile(event.target.files[0]));document.querySelector('#markdown-text')?.addEventListener('input',event=>updatePaste(event.target.value));const drop=document.querySelector('#dropzone');drop?.addEventListener('keydown',event=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();document.querySelector('#file-input').click();}});drop?.addEventListener('dragover',event=>{event.preventDefault();drop.classList.add('dragging');});drop?.addEventListener('dragleave',()=>drop.classList.remove('dragging'));drop?.addEventListener('drop',event=>{event.preventDefault();if(event.dataTransfer.files.length!==1){toast('请一次选择一份 Markdown 文件。');return;}chooseFile(event.dataTransfer.files[0]);});document.querySelector('#report-title')?.addEventListener('input',event=>{state.title=event.target.value;state.pending=null;});document.querySelector('#report-week')?.addEventListener('input',event=>{state.week=event.target.value;state.pending=null;});document.querySelector('#post-sort')?.addEventListener('change',event=>{state.sort=event.target.value;state.browsePage=1;render();});document.querySelector('#browse-page-size')?.addEventListener('change',event=>{state.pageSize=Number(event.target.value)||10;state.browsePage=1;render();});document.querySelector('#browse-page-number')?.addEventListener('change',event=>{const totalPages=Math.max(1,Math.ceil(visiblePosts().posts.length/state.pageSize));state.browsePage=Math.max(1,Math.min(totalPages,Number(event.target.value)||1));render();});document.querySelector('#upload-form')?.addEventListener('submit',submit);app.querySelectorAll('[data-action]').forEach(button=>button.addEventListener('click',()=>action(button.dataset.action)));app.querySelectorAll('[data-record]').forEach(button=>button.addEventListener('click',()=>openRecord(Number(button.dataset.record))));app.querySelectorAll('[data-post]').forEach(button=>button.addEventListener('click',()=>openPost(Number(button.dataset.post))));app.querySelectorAll('[data-page]').forEach(button=>button.addEventListener('click',()=>{const totalPages=Math.max(1,Math.ceil(visiblePosts().posts.length/state.pageSize));state.browsePage=Math.max(1,Math.min(totalPages,state.browsePage+(button.dataset.page==='next'?1:-1)));render();}));app.querySelectorAll('[data-author-filter]').forEach(node=>{const filter=()=>{state.authorFilter=node.dataset.authorFilter||'';state.browsePage=1;state.route='others';history.replaceState(null,'','#others');render();};node.addEventListener('click',event=>{event.stopPropagation();filter();});node.addEventListener('keydown',event=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();event.stopPropagation();filter();}});}); }
document.addEventListener('click', event => { const button = event.target.closest('[data-reset-user]'); if (!button) return; const input = document.querySelector('#new-username'); if (input) input.value = button.dataset.resetUser; document.querySelector('#new-password')?.focus(); toast('填写新密码并保存，即可重置该成员密码。'); });
async function enter(user) { const visitor=isVisitor(user); Object.assign(state,{user:{...user,login:user.username},route:visitor?'others':'mine',sharedError:'',mineError:'',error:''});if(!visitor)await loadMine();await loadShared();history.replaceState(null,'',visitor?'#others':'#mine'); }
async function connect(event) { event.preventDefault();if(state.busy)return;const mode=document.querySelector('#login-mode')?.value||'member';const name=document.querySelector('#username')?.value.trim()||'';const password=document.querySelector('#password')?.value||'';if(document.querySelector('#password'))document.querySelector('#password').value='';state.busy=true;state.error='';render();try{await enter(await service.login(name,password,mode));}catch(error){state.user=null;state.error=error.message;}finally{state.busy=false;render();} }
function adminPage() { if(!isManager(state.user))return ''; return `<div class="page-heading"><div><div class="eyebrow">MEMBERS</div><h1>成员管理<span class="heading-dot">.</span></h1><p>由你创建账号，再把用户名和密码分别交给成员。</p></div></div><section class="panel admin-panel"><h2>添加成员 / 重置密码</h2><form id="member-edit-form"><div class="form-row"><div class="field"><label for="new-username">用户名</label><input id="new-username" required pattern="[a-z][a-z0-9_-]{1,31}" placeholder="例如 zhangsan" autocomplete="off"></div><div class="field"><label for="new-name">姓名</label><input id="new-name" maxlength="60" placeholder="例如 张三"></div></div><div class="form-row"><div class="field"><label for="new-role">角色</label><select id="new-role"><option value="graduate">研究生</option><option value="postdoc">博士后</option><option value="pi">PI</option><option value="visitor">游客</option></select></div><div class="field"><label for="new-password">初始密码 / 新密码</label>${passwordField('new-password','new-password','至少 6 个字符')}</div></div><p class="form-note">填写已有用户名会重置密码并更新角色。密码不会显示或恢复旧密码。</p>${errorBox(state.error)}<button class="button primary" ${state.busy?'disabled':''}>${state.busy?'正在保存…':'保存成员账号'}</button></form></section><section class="history-section"><div class="section-heading"><h2>已创建的账号</h2><button class="button text-button" data-action="members">刷新名单</button></div><div class="record-list">${(state.members||[]).map(u=>`<div class="record-row"><span class="avatar">${e(Array.from(u.name||u.username)[0])}</span><span class="record-copy"><strong>${e(u.name||u.username)}</strong><small>${e(u.username)} · ${e(roleLabel(u.role))} · ${u.active?'已启用':'已停用'}</small></span>${u.id!==state.user.id?`<button class="button text-button" data-reset-user="${e(u.username)}">重置密码</button><button class="button secondary" data-toggle-user="${e(u.username)}">${u.active?'停用':'启用'}</button><button class="button text-button danger" data-delete-user="${e(u.username)}">删除</button>`:''}</div>`).join('')}</div></section>`; }
async function loadMembers(){if(!isManager(state.user))return;state.busy=true;state.error='';render();try{state.members=await service.request('/api/admin/users');}catch(error){state.error=error.message;}finally{state.busy=false;render();}}
async function saveMember(event){event.preventDefault();if(state.busy)return;const body={username:document.querySelector('#new-username').value.trim(),name:document.querySelector('#new-name').value.trim(),role:document.querySelector('#new-role').value,password:document.querySelector('#new-password').value};document.querySelector('#new-password').value='';state.busy=true;state.error='';render();try{await service.request('/api/admin/users',body);state.members=await service.request('/api/admin/users');toast('成员账号已保存。请分发用户名和密码。');}catch(error){state.error=error.message;}finally{body.password='';state.busy=false;render();}}
async function toggleMember(name){if(state.busy)return;state.busy=true;state.error='';try{await service.request('/api/admin/users',{username:name,action:'toggle'});state.members=await service.request('/api/admin/users');}catch(error){state.error=error.message;}finally{state.busy=false;render();}}
async function deleteMember(name){if(state.busy||!window.confirm(`确定删除账号“${name}”？已提交的报告不会删除。`))return;state.busy=true;state.error='';try{await service.request('/api/admin/users',{username:name,action:'delete'});state.members=await service.request('/api/admin/users');toast('账号已删除。');}catch(error){state.error=error.message;}finally{state.busy=false;render();}}
function openRecord(index){if(state.busy)return;state.reading=state.records[index];state.readFrom='mine';state.route='read';render();window.scrollTo(0,0);document.querySelector('#main').focus();}
async function openPost(index){if(state.busy)return;const post=state.posts[index];state.busy=true;try{const content=await service.file(post.path);state.reading={...post,content};state.readFrom='others';state.route='read';render();window.scrollTo(0,0);document.querySelector('#main').focus();}catch(error){toast(error.message);}finally{state.busy=false;document.querySelector('#main')?.setAttribute('aria-busy','false');}}

// Updated copy and date based submission layout. Kept here to preserve compatibility with the original demo functions above.
function login() { const form=`<form id="member-form"><div class="login-identity-row"><select id="login-mode" aria-label="身份"><option value="member">课题组成员</option><option value="visitor">游客</option></select><input id="username" autocomplete="username" required placeholder="管理员分配的用户名"></div><label for="password" class="spaced-label">密码</label>${passwordField("password","current-password","管理员分配的密码")}${errorBox(state.error)}<button id="login-submit" class="button primary wide" type="submit" ${state.busy?"disabled":""}>${state.busy?"正在登录…":"登录"} ${icon("arrow")}</button><p class="form-note">课题组内部学术交流专用</p></form>`; return `<main class="login-layout" id="main"><section class="login-story"><div class="brand">${brand}</div><div class="story-body"><div class="eyebrow">${e(config.groupName||'MLCCLab')} 思路笔记</div><h1>让思考留下痕迹，<br>让下一步更清楚。</h1><p>记录一个问题、一点认识的变化，<br>或一个还没想明白的地方。</p><div class="story-note"><span>深度思考从这里开始</span><p>“我想弄清楚什么，<br>什么证据改变了我的想法？”</p><div class="note-rule"></div><small>不必等到有了结论才记录。</small></div></div><div class="story-footer">个人记录 · 组内交流 · 持续积累</div></section><section class="login-panel"><div class="login-card"><div class="eyebrow blue">欢迎回来</div><h2>进入你的思考空间</h2><p class="muted muted-spacer" aria-hidden="true">&nbsp;</p>${form}</div><footer>GroupIdea <span>Copyright 2026 Jingbai Li All Rights Reserved.</span></footer></section></main>`; }
function shell() { const mine=state.route==='mine'||(state.route==='read'&&state.readFrom==='mine'); return `<div class="workspace"><aside class="sidebar"><a class="brand" href="#mine" aria-label="GroupIdea 首页">${brand}</a><div class="workspace-label">${e(config.groupName||'课题组')}思考空间</div><nav aria-label="主要导航">${isVisitor(state.user)?'':`<a href="#mine" class="nav-link ${mine?'active':''}">${icon('book')} 我的想法</a>`}<a href="#others" class="nav-link ${state.route==='others'||(state.route==='read'&&state.readFrom==='others')?'active':''}">${icon('people')} 看看别人怎么想</a>${isManager(state.user)?`<a href="#admin" class="nav-link ${state.route==='admin'?'active':''}">${icon('people')} 成员管理</a>`:''}</nav><div class="sidebar-bottom"><div class="privacy-note">${icon('check')}<span>原始记录仅提交给导师<br>整理后再在组内分享</span></div><div class="user-block"><span class="avatar">${e(Array.from(state.user.name||state.user.login)[0])}</span><span class="user-name">${e(state.user.name||state.user.login)}<small>${e(state.user.login)} · ${e(roleLabel(state.user.role))}</small></span><button class="icon-button" data-action="logout" aria-label="退出登录" title="退出">${icon('exit')}</button></div></div></aside><div class="main-wrap"><header class="topbar"><span>${state.route==='admin'?'成员管理':mine?'个人记录':'组内交流'}</span><span class="mode-tag"><span class="status-dot"></span>私有工作空间</span></header><main id="main" class="content" tabindex="-1" aria-busy="${state.busy}">${state.route==='admin'?adminPage():state.route==='read'?reader():state.route==='others'?others():minePage()}</main><footer class="workspace-footer"><span>GroupIdea · 每一点思考，都值得留下</span><span>时间显示：北京时间</span></footer></div></div>`; }
function minePage() { const hasFile=!!state.file; return `<div class="page-heading"><div><div class="eyebrow">MY IDEAS</div><h1>我的想法<span class="heading-dot">.</span></h1><p>留下此刻最重要的一点思考。</p></div><button class="button secondary" data-action="template">${icon('download')} 下载记录模板</button></div><div class="write-grid"><section class="panel upload-panel"><div class="section-heading"><h2>提交一份新记录</h2><span class="subtle">Markdown</span></div><form id="upload-form"><div class="field idea-title-field"><label for="report-title">记录标题</label><input id="report-title" maxlength="100" value="${e(state.title)}" placeholder="此刻，你在思考什么" required></div><input id="file-input" class="visually-hidden" type="file" accept=".md,.markdown,text/markdown" aria-label="选择 Markdown 文件"><label for="file-input" id="dropzone" class="dropzone ${hasFile?'selected':''}" tabindex="0" role="button">${icon(hasFile?'file':'upload')}<strong>${hasFile?e(state.file.name):'拖入 Markdown 文件，或点击选择'}</strong><span>${hasFile?`${(state.file.size/1024).toFixed(1)} KB · 点击更换文件`:'支持 .md / .markdown，最大 512 KB'}</span></label><div class="paste-field"><label for="markdown-text">或直接粘贴 Markdown 文本</label><textarea id="markdown-text" rows="18" placeholder="${e(weeklyTemplate)}">${e(state.file?.content||'')}</textarea></div>${errorBox(state.error)}<div class="upload-actions"><span>提交人：<strong>${e(state.user.name||state.user.login)}</strong></span><div><button class="button text-button" type="button" data-action="preview" ${!hasFile||state.busy?'disabled':''}>${state.preview?'收起预览':'预览正文'}</button><button class="button primary" type="submit" ${!hasFile||state.busy?'disabled':''}>${state.busy?'正在提交…':'提交记录'} ${icon('arrow')}</button></div></div><p class="form-note submission-note">再次提交会保留为新记录，随后会分享到组内思考</p></form>${state.preview&&state.file?`<div class="inline-preview markdown">${renderMarkdown(state.file.content)}</div>`:''}</section><aside class="thinking-note"><span class="small-number">随时随刻 · 5–10 分钟</span><h2>从一个小问题开始</h2><ol><li>此刻最想弄清楚什么？</li><li>什么证据改变了我的想法？</li><li>下一步，我想先试什么？</li></ol><p>还没有答案，也可以记录。<br>不确定的地方，就是讨论的起点。</p></aside></div><section class="history-section"><div class="section-heading"><h2>我的提交记录 <span class="count">${state.records.length}${state.files.length>state.records.length?'+':''}</span></h2><button class="button text-button" data-action="refresh">刷新记录</button></div>${errorBox(state.mineError)}${state.records.length?`<div class="record-list">${state.records.map((r,i)=>`<button class="record-row ${r.archived?'archived':''}" data-record="${i}"><span class="file-badge">${icon('file')}</span><span class="record-copy"><strong>${e(r.title)}</strong><small>${e(recordLabel(r))} <span>·</span> ${e(dateLabel(r.submittedAt))}</small></span><span class="record-status">${r.archived?'已归档':'已提交'}</span>${icon('arrow')}</button>`).join('')}</div>${state.files.length>state.page*10?'<button class="button secondary load-more" data-action="more">加载更早记录</button>':''}`:state.mineError?'':empty('还没有提交记录','选一份 Markdown，留下此刻的想法。')}</section>`; }
function others() { const {posts:allPosts,totalPages}=visiblePosts();const start=(state.browsePage-1)*state.pageSize;const posts=allPosts.slice(start,start+state.pageSize);const authorPost=state.authorFilter?state.posts.find(post=>post.author===state.authorFilter):null;const heading=state.authorFilter?`作者：${e(displayAuthor(authorPost||{author:state.authorFilter}))}`:'最近的思考';const pager=allPosts.length?`<div class="pagination" aria-label="分页"><button class="button secondary" data-page="prev" ${state.browsePage<=1?'disabled':''}>上一页</button><label>第 <input id="browse-page-number" type="number" min="1" max="${totalPages}" value="${state.browsePage}"> / ${totalPages} 页</label><button class="button secondary" data-page="next" ${state.browsePage>=totalPages?'disabled':''}>下一页</button><label class="page-size-label">每页 <select id="browse-page-size"><option value="10" ${state.pageSize===10?'selected':''}>10</option><option value="50" ${state.pageSize===50?'selected':''}>50</option><option value="100" ${state.pageSize===100?'selected':''}>100</option></select> 篇</label></div>`:'';return `<div class="page-heading"><div><div class="eyebrow">SHARED IDEAS</div><h1>看看别人怎么想<span class="heading-dot">.</span></h1></div><button class="button secondary" data-action="refresh">刷新内容</button></div><div class="section-heading feed-heading"><h2>${heading}</h2><div class="browse-actions">${state.authorFilter?'<button class="button text-button" data-action="clear-author-filter">返回全部</button>':''}<label class="sort-control">排序 <select id="post-sort"><option value="time" ${state.sort==='time'?'selected':''}>按时间</option><option value="role" ${state.sort==='role'?'selected':''}>按角色</option><option value="name" ${state.sort==='name'?'selected':''}>按姓名</option></select></label></div></div>${errorBox(state.sharedError)}${allPosts.length?`<section class="post-list">${posts.map((p,i)=>`<button class="post-card" data-post="${state.posts.indexOf(p)}"><span class="post-number">${String(start+i+1).padStart(2,'0')}</span><span class="post-body"><strong>${e(p.title)}</strong><span class="post-meta"><span class="author-chip author-filter" data-author-filter="${e(p.author||'')}" role="button" tabindex="0">${e(displayAuthor(p))}</span><span>${e(dateLabel(p.publishedAt))} 发布</span></span>${p.excerpt?`<span class="excerpt">${e(p.excerpt)}</span>`:''}</span><span class="post-arrow">${icon('arrow')}</span></button>`).join('')}</section>${pager}`:state.sharedError?'':empty(state.authorFilter?'没有找到该作者的分享':'这里等待第一份分享','')}`; }
async function loadMine(reset=true) { state.mineError=''; try { if(reset){state.files=await service.listReports();state.records=[];state.page=0;} const batch=state.files.slice(state.page*10,(state.page+1)*10); const results=await Promise.allSettled(batch.map(f=>service.readReport(f.path))); let failed=0; for(const result of results){if(result.status==='fulfilled')state.records.push(result.value);else{failed++;state.mineError=result.reason.message;}} state.page++;state.records.sort((a,b)=>String(b.submittedAt).localeCompare(String(a.submittedAt)));if(failed)state.mineError=`${failed} 份记录未能读取。${state.mineError} 可点击刷新重试。`;}catch(error){state.mineError=error.message;} }
async function submit(event){event.preventDefault();if(state.busy||!state.file)return;try{state.pending ||= makeReport({user:state.user,title:state.title,filename:state.file.name,content:state.file.content});state.busy=true;state.error='';render();await service.submit(state.pending);state.file=null;state.title='';state.pending=null;state.preview=false;await loadMine();toast('提交成功，原稿已保存。');}catch(error){state.error=error.message;}finally{state.busy=false;render();}}
async function action(name){if(state.busy){toast('正在处理，请稍候。');return;}if(name==='logout'){try{await service.logout();}catch(error){toast(error.message);}state={...state,user:null,restoring:false,route:'mine',file:null,title:'',week:currentWeek(),records:[],posts:[],files:[],page:0,busy:false,preview:false,error:'',mineError:'',sharedError:'',pending:null,sort:'time',authorFilter:'',browsePage:1,pageSize:10,members:[]};history.replaceState(null,'',location.pathname);render();return;}if(name==='members'){await loadMembers();return;}if(name==='template'){download(weeklyTemplate,'思考记录模版.md');return;}if(name==='preview'){state.preview=!state.preview;render();return;}if(name==='clear-author-filter'){state.authorFilter='';state.browsePage=1;render();return;}if(name==='back'){state.route=state.readFrom;render();return;}if(name==='download'){download(state.reading.content,state.reading.filename||`${state.reading.title}.md`);return;}if(name==='refresh'||name==='more'){state.busy=true;if(state.route==='others')await loadShared();else await loadMine(name!=='more');state.busy=false;render();}}
function reader() { const r=state.reading; const published=dateLabel(r.publishedAt||r.submittedAt); return `<div class="reader-toolbar"><button class="button text-button" data-action="back">${icon('back')} 返回${state.readFrom==='mine'?'我的想法':'分享列表'}</button><button class="button secondary" data-action="download">${icon('download')} 下载 Markdown</button></div><article class="reader"><div class="eyebrow">${state.readFrom==='mine'?'MY RECORD':'SHARED NOTE'}</div><h1>${e(r.title)}</h1><div class="reader-meta"><span class="author-chip author-filter" data-author-filter="${e(r.author||'')}" role="button" tabindex="0">${e(displayAuthor(r))}</span><span>${e(published)} 发布</span></div><div class="markdown">${renderMarkdown(r.content)}</div></article>`; }
window.addEventListener('hashchange',()=>{if(!state.user||state.busy)return;state.route=location.hash==='#admin'&&isManager(state.user)?'admin':location.hash==='#others'||isVisitor(state.user)?'others':'mine';if(state.route==='admin')loadMembers();else render();});
window.addEventListener('beforeunload',event=>{if(state.file||state.busy){event.preventDefault();event.returnValue='';}});
async function restoreSession(){if(!service.accessToken){state.restoring=false;return;}state.busy=true;render();try{await enter(await service.me());}catch{service.clearSession();state.user=null;}finally{state.restoring=false;state.busy=false;render();}}
render();
restoreSession();
